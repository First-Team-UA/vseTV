'use client';

import { Btn } from '../../styles/login/Login.styled';
import React from 'react';

const SubmitButton: React.FC = () => {
  return <Btn type="submit">Login</Btn>;
};

export default SubmitButton;
